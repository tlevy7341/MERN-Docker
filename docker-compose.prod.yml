version: "3"
services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile.prod
    ports:
      - 3001:3001
    restart: unless-stopped
    depends_on:
      - mongo
  client:
    build: 
      context: ./client
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
    depends_on:
      - server
    restart: unless-stopped
  mongo:
    container_name: mongo
    image: mongo:latest
    hostname: mongo
    restart: always
    ports:
        - 27017:27017
    volumes:
        - mongodbdata:/data/db
volumes:
  mongodbdata:
      driver: local